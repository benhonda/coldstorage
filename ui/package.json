{
  "name": "coldstorage-ui",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "ColdStorage control panel (Electron/React). Layer 1: Node IPC bridge over the coldstored control socket.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "prove": "bun run src/daemon/prove.ts"
  },
  "devDependencies": {
    "@types/node": "latest",
    "typescript": "latest"
  }
}
