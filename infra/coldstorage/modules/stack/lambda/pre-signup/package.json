{
  "name": "coldstorage-pre-signup",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "build": "bun build index.ts --target=node --format=esm --external @aws-sdk/* --outfile dist/index.mjs"
  },
  "devDependencies": {
    "@aws-sdk/client-cognito-identity-provider": "^3.1089.0",
    "@types/aws-lambda": "^8.10.162",
    "@types/bun": "^1.3.14",
    "@types/node": "^26.1.1",
    "typescript": "^7.0.2"
  }
}
