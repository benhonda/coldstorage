# Neon connection string (Neon dashboard → your project → Connection Details)
DATABASE_URL=

# Paddle (dashboard → Developer tools → Notifications / Authentication)
PADDLE_WEBHOOK_SECRET=
PADDLE_API_KEY=
PADDLE_ENVIRONMENT=sandbox

# Cognito (from `cd infra/coldstorage/live/production && terragrunt output` — public, not secret)
COGNITO_USER_POOL_ID=
COGNITO_USER_POOL_CLIENT_ID=
